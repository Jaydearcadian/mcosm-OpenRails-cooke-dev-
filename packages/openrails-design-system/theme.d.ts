export declare const openRailsTheme: Readonly<{
  colors: Readonly<Record<'canvas' | 'surface' | 'ink' | 'dark' | 'muted' | 'line' | 'lineSoft' | 'panel' | 'signal', string>>;
  typography: Readonly<{ editorial: string; technical: string }>;
  provenance: readonly ['DEMONSTRATION', 'RECORDED', 'LIVE'];
}>;
export declare const openRailsSurfaces: Readonly<Record<'narrative' | 'systemLab' | 'network' | 'build' | 'docs' | 'dashboard', string>>;
