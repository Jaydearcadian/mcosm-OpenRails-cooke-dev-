# openrails stream-status

Read stream registry state for a paycard.

```bash
openrails stream-status --rpc-url <url> --hub <address> --paycard-id <bytes32>
```

This command is read-only. Add `--dry-run` to validate inputs without reading RPC state.
